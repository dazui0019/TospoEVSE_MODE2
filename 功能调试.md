# 功能调试记录

## ADC部分

1. 交流输入电压采样
2. 温度采样
3. 继电器粘连检测
4. 充电电流采样
5. CP电压采样

其中`交流输入电压采样`、`继电器粘连检测`和`充电电流采样`放在同一个组里进行触发，需要使用`DMA`。

### 交流输入电压采样

- 采样方案:
  - 按固定20ms(50Hz)周期触发一次ADC，每次采样100个数据点。
- 问题:
  - 需要修改到`ADC0`上。
  - 在目前的方案中，50Hz的采样频率，只能正确采样50Hz的交流电，后面可以考虑开启一个较大的DMA缓冲区，由`FREQ_IN`来控制定时器的开启和停止，DMA缓冲区已使用的大小可以反映出交流信号的周期和频率。

### 温度采样

- 采样方案:
  - 放在注入通道，每隔500ms由软件触发一次转换。
- 问题:
  - 改成定时器触发可能更好一点。

### 继电器粘连检测

- 采样方案: 与交流输入电压采样相同
- 问题: 与交流输入电压采样相同

### 充电电流采样

- 采样方案: 与交流输入电压采样相同
- 问题:
  - 采样值波动较大，其他与与交流输入电压采样相同
